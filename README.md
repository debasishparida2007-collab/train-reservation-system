# train-reservation-system
A fully command-line executable Train Ticket Reservation System built in Java. Features thread-safe bookings, custom exception handling, and terminal-based CRUD operations.
